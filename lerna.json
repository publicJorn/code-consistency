{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "npmClient": "pnpm",
  "version": "independent",
  "ignoreChanges": ["**/*.test.*", "**/*.md"],
  "command": {
    "publish": {
      "message": "build(repo): publish"
    }
  }
}
